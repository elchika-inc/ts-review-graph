{
  "name": "ts-review-graph-workspace",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "sync-version": "node scripts/sync-version.mjs"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
