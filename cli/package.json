{
  "name": "ts-review-graph",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "ts-review-graph": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@ts-review-graph/core": "workspace:*",
    "@ts-review-graph/mcp-server": "workspace:*",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
